const linkedListFactory = () => {

    let head = null;
    let tail = null;

    const append = (value) => 
}

const nodeFactory = (value, next) => {

    value = null
    next = null

    return {
        value,
        nextNode
    }

}

const Node = nodeFactory()
console.log(Node.nextNode)